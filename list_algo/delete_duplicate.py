#here, i just use a set collection, so easy !

list_names = ["Pat", "Rick", "Frank", "Bryan", "Scott", "Scott", "Peter", "Bryan"]

set_names = set(list_names)
print(set_names)

#be careful, because the order isn't respect when we use set